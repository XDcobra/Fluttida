source 'https://rubygems.org'

# Pin CocoaPods to a version compatible with the Flutter podhelper used by the
# project's Flutter SDK. Adjust this version if CI still fails; 1.11.3 is a
# conservative choice that is widely compatible with Flutter toolings.
gem 'cocoapods', '1.11.3'
