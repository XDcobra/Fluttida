# Example key.properties (do NOT commit real secrets)
# Copy to: example_app/fluttida/android/key.properties
# Then fill with your local values. In CI, this file is generated dynamically.

# Keystore password
storePassword=CHANGE_ME
# Key password (often same as storePassword)
keyPassword=CHANGE_ME
# Alias of the key inside your keystore
keyAlias=fluttida_key
# Relative path to the keystore from the android/ folder
# For local builds, place your keystore at: example_app/fluttida/android/app/release.keystore
storeFile=app/release.keystore
